# Lanes A/B/C static outcomes — 2026-08-13 (order 13j, predict-then-land, ZERO builds)

## Lane A — §6.4 gen-membership PREDICTION GATE: CLEARED (build pre-authorized)

**Own-gen mid-block-taken-edge fraction under FUNCTION-level clustering = 100%.**
Static CFG pass on the MP4 decomp (build/GMPE01_01/main.dol + gmpe01_full.map), top-120
functions by board sample share (~91% coverage): all 1191 forward-conditional taken edges
(bc, op==16, target>PC) stay WITHIN the enclosing function; zero cross-function. Clustering =
each function -> one gen: 92 gens, largest 436 blocks (SetTevStageTex 9608B) << the 6144-block
cap. Weighted by func_samples/est_blocks.

**Why this matters — it explains the neutral N-fn generalization (2026-08-13, reverted):** the
CURRENT N-fn gens cluster by DISCOVERY ORDER, so mid-block taken targets land CROSS-gen and fall
to host_return (census: taken 212M -> host_return 162->198M, direct only +8M; frames neutral).
Cluster by FUNCTION (decomp-CFG-driven) and every taken target is OWN-gen -> chains via the
internal-table return_call_indirect. 100% >> 80% threshold.

**PRE-AUTHORIZED — the ONE §6.4-complete build (next cooled window), both pieces together:**
1. Global-fallback chain on emit_chain_or_return's region_gen>=0 path (port the merged path's
   return_call_indirect on g_bem_disp_tag/slot, ppc_emit.cpp ~405-432) — the cross-gen safety.
2. Decomp-CFG function-level gen membership: batch each hot function's blocks into ONE gen
   (from the decomp CFG), replacing discovery-order clustering, so taken targets are own-gen by
   construction. + re-enable the N-fn taken-exit generalization (jit_branch.cpp emit_coalesced_
   taken_exit, gated merged||region_gen>=0, reverted 2026-08-13 pending this).
Gate: interleaved frames A/B (cooled, thermal-sentinel-bracketed) + traps=0 + renders-intact;
predicted to flip N-fn from neutral to a win. Also close the two §6.4 correctness holes first
(PM54d unguarded-after-evict gen-tag term; PM54f fusion-extent invalidation — the latter also a
§6.2 precondition).

## Lane C — §6.2 fusion EXECUTION CONTRACT (complete, ready when §6.4 gates)

| fn | share | class | guard plan | goldens |
|---|---|---|---|---|
| HandleReverb 0x801136f0 | 6.44% | SINGLE_ONLY (scalar-FP reverb) | single-arm at entry, no routing | MusyX ABI, board goldens exist |
| PSMTXROMultVecArray 0x800bc8d0 | (matrix) | SINGLE_ONLY (paired-single) | emit psq_* directly, ps_native_off never fires | 26/26 psmtx_goldens.json |
| C_MTXOrtho 0x800bbeb4 | | SINGLE_ONLY (scalar-FP ortho) | monolithic single | SDK math_mtx34.c |
| PSMTXInverse 0x800bb57c | | **DUAL_SEGMENT** | split guard at MSR.FP: single ps_mul loop + double fdivs/frsqrte epilog, PER-SEGMENT (not union — the step-0 −38% IDCT anti-pattern) | SDK 3x3 cofactor |
| FaceDraw 0x80014fdc | 5.2% | FP-FREE -> A3.2, NOT fusion | — | render-correctness gate |

Fusion starts with the SINGLE_ONLY set (no dual-arm complexity); PSMTXInverse needs the
per-segment guard. Goldens by direct SDK invocation. The minigame savestate
`~/Downloads/MarioParty4 (3).gcs.gz` is the skinning-scene arm-fires attribution vehicle.

## Lane B — SAB pump: mechanism gate FAILED (NOT built, needs static re-scope)

The scoped de-quant ("route yields entirely through MessageChannel when GPU work pending")
would reintroduce the PM54 present-freeze: worker_funcs.js:233-236 records that pure-MessageChannel
STARVES the timer source, and Dawn's device ticks / readback completions are TIMER-scheduled —
GPU-work-pending is exactly when the setTimeout slot is needed. Corrected direction: skip
setTimeout only during GPU-IDLE stretches. And SAB is GPU-ACTIVE (~30ms device work), so a
yield-quantum de-quant targets menu/idle overhead, not SAB gameplay's bottleneck (the cross-worker
handshake round-trip in the ~172ms turnaround). Re-scope statically — what's actually in the 172ms —
before this earns a build. The 5.7->7.7-11.2 prediction needs re-derivation against the true mechanism.
