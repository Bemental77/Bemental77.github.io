diff --git a/core/build.h b/core/build.h
index 6ea5355..41e4120 100755
--- a/core/build.h
+++ b/core/build.h
@@ -17,6 +17,7 @@
 #define CPU_ARM      0x20000002
 #define CPU_ARM64    0x20000003
 #define CPU_X64      0x20000004
+#define CPU_WASM     0x20000005
 
 //FEAT_SHREC, FEAT_AREC, FEAT_DSPREC
 #define DYNAREC_NONE	0x40000001
@@ -24,7 +25,9 @@
 
 //automatic
 
-#if defined(__x86_64__) || defined(_M_X64)
+#if defined(__EMSCRIPTEN__)
+	#define HOST_CPU CPU_WASM
+#elif defined(__x86_64__) || defined(_M_X64)
 	#define HOST_CPU CPU_X64
 #elif defined(__i386__) || defined(_M_IX86)
 	#define HOST_CPU CPU_X86
@@ -59,6 +62,14 @@
 #define FEAT_DSPREC DYNAREC_NONE
 #endif
 
+#if defined(__EMSCRIPTEN__)
+// WASM target: SH4 dynarec via WasmDynarec (rec_wasm.cpp + bementalJIT).
+// ARM7 (AICA) and DSP fall back to interpreters.
+#define FEAT_SHREC DYNAREC_JIT
+#define FEAT_AREC DYNAREC_NONE
+#define FEAT_DSPREC DYNAREC_NONE
+#endif
+
 #ifdef __SWITCH__
 #define FEAT_NO_RWX_PAGES
 #endif
