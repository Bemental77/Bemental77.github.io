diff --git a/core/linux/context.cpp b/core/linux/context.cpp
index 461ff26..0a5d1ba 100644
--- a/core/linux/context.cpp
+++ b/core/linux/context.cpp
@@ -130,6 +130,10 @@ static void context_segfault(host_context_t* hostctx, void* segfault_ctx)
     #else
         #error "Unsupported OS"
 	#endif
+#elif HOST_CPU == CPU_WASM
+    // wasm has no host signal-handler context; JIT-emitted blocks live in
+    // WebAssembly.Module instances and trap as wasm exceptions, not SIGSEGV.
+    (void)hostctx; (void)segfault_ctx;
 #else
 	#error Unsupported HOST_CPU
 #endif
